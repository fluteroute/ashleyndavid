import React from 'react';
import { render } from '../../../utils/setupTests';
import { Divider } from './Divider';

test('[components:base] Divider: should render', () => {
  render(<Divider />);
});
